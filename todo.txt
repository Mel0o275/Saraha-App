1)Login (Account Ban on Failed Attempts)

If a user enters an incorrect password 5 consecutive times, their account should be temporarily banned for 5 minutes.

After 5 minutes, the user should be able to try logging in again. => DONE

2) 2-step-verification:

• Implement a 2-step-verification enabling endpoint: => DONE

If a user wants to enable 2 step verification on his account, he will call this endpoint while logged

in then an OTP will be sent to his account.

o With that OTP he needs to send it to another endpoint to verify that OTP and enable the 2-step- verification on his account

• In the Login endpoint: => DONE

When a user is trying to login, (only if he enabled 2-step-verification) after validating his data is

correct send a verification code (OTP) to his email to validate the identity of the user trying to login In another endpoint (login confirmation) the user is asked to send a verification code to this endpoint and after validating the OTP is correct the user receives their tokens

if the user has 2-step-verification disabled the login should validate the login data and send the

user their tokens without extra steps

3) Update Password (Authenticated User Only) => DONE

4) Forget Password => DONE

Implement a Forget Password feature that allows users to reset their password.

5) Bonus: Delete Unconfirmed Users Automatically => DONE

Any user who does not confirm their email within 24 hours should be automatically deleted.

This can be implemented using one of the following:

• TTL (Time-To-Live) using Mongoose TTL index

Redis TTL

o CRON Job that runs periodically to delete expired unconfirmed users